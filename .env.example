# Postgres do Railway (use a URL interna quando o app tambem estiver no Railway)
DATABASE_URL=postgresql://usuario:senha@host:5432/banco

# Acesso ao painel /admin
ADMIN_USER=admin
ADMIN_PASSWORD=troque-esta-senha

# Porta local (Railway define PORT automaticamente em producao)
PORT=3000
